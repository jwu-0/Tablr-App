/**
 * React Query hooks for the reservations domain.
 *
 * STUB: no hooks yet. They arrive with the matching vertical slice — see
 * TABLR_PLAN.md. Call `api` from '@/lib/api'; never fetch
 * directly and never reach for the Supabase client here.
 */
export {};
