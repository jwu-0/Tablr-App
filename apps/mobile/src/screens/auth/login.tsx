/**
 * Log in with .edu email + password (Supabase Auth).
 * Filled in by slice 1 (accounts + auth).
 */
import { ComingSoon } from '@/components/coming-soon';

export function LoginScreen() {
  return <ComingSoon title="Log in" note="Supabase email + password sign-in lands in slice 1." />;
}
