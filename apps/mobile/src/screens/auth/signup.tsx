/**
 * Sign up with a .edu email (Supabase Auth), then hand off to /verify.
 * Filled in by slice 1 (accounts + auth).
 */
import { ComingSoon } from '@/components/coming-soon';

export function SignupScreen() {
  return <ComingSoon title="Sign up" note="Requires a .edu address." />;
}
