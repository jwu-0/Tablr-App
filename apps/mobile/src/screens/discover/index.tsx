/**
 * Discover — ranked restaurants and open tables near the user.
 * Filled in by slices 4 (restaurants) and 5 (matching).
 */
import { ComingSoon } from '@/components/coming-soon';

export function DiscoverScreen() {
  return <ComingSoon title="Discover" note="Restaurants and open tables near you." />;
}
