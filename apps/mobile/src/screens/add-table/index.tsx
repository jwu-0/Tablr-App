/**
 * Add Table — create a table: restaurant, time, seats, vibe tags.
 * Filled in by slice 6 (tables/groups). The Figma design celebrates a created
 * table with confetti; no RN confetti library is installed yet, so pick one
 * (it will need a development build) or use a lighter Reanimated flourish.
 */
import { ComingSoon } from '@/components/coming-soon';

export function AddTableScreen() {
  return <ComingSoon title="Add a Table" note="Pick a spot, a time, and who you'd like to meet." />;
}
