/**
 * Profile — the user's own profile, interests, and settings.
 * Filled in by slice 2 (profiles + interest taxonomy).
 */
import { ComingSoon } from '@/components/coming-soon';

export function ProfileScreen() {
  return <ComingSoon title="Profile" note="Your interests, cuisines, and settings." />;
}
