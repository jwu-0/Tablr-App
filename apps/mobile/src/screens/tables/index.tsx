/**
 * My Tables — tables the user created, joined, or was invited to.
 * Filled in by slice 6 (tables/groups).
 */
import { ComingSoon } from '@/components/coming-soon';

export function TablesScreen() {
  return <ComingSoon title="My Tables" note="Tables you've joined or created." />;
}
