/**
 * Friends — people met at past tables, requests, and invites.
 * Filled in after the core loop closes.
 */
import { ComingSoon } from '@/components/coming-soon';

export function FriendsScreen() {
  return <ComingSoon title="Friends" note="People you've shared a table with." />;
}
